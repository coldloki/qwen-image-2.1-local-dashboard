# Token for pushing to the qwen-studio public mirror repo.
# Generate one at: https://github.com/settings/tokens
# Only needs the 'repo' scope.
# After the push, the script logs gh back out — but rotate the token anyway
# on your next push if you suspect it leaked anywhere.
GH_TOKEN=ghp_paste_your_token_here
